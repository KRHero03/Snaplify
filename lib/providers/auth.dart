import 'package:flutter/material.dart';

class Auth with ChangeNotifier {
  // add more data as provided by google signin
  String userName, userId;

  // call this function to store the user data to firebase
  Future<void> login() {
    // i will implement this just edit the parameters
  }
}
